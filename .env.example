# Turso libSQL — PRIMARY URL ONLY (never a replica — read-after-write).
TURSO_DATABASE_URL="libsql://your-db-primary.turso.io"
TURSO_AUTH_TOKEN="your-token"

# Optional: defaults for money formatting (used as fallbacks).
PUBLIC_MONEY_LOCALE="en-US"
PUBLIC_MONEY_CURRENCY="USD"